#include <iostream>
using namespace std;
int main()
{
    int a,b;
    cout<<"enter the two no.";
    cin>>a>>b;
    if(a>b)
    cout<<"the entered no is maximum"<<a;
    else
    cout<<"the no is minimum"<<b;
    return 0;

}